- if current_user && current_user != user
  #follow_form
    - if current_user.following?(current_user, user)
      = render partial: "common/unfollow", locals:{ user: user }
    - else
      = render partial: "common/follow", locals: { user: user }
